import { idDate } from "./idDate"
import { generator } from "./generator"
import { transactionsRewards } from "./transactionsRewards"

export const blockColumns = {
  idDate,
  generator,
  transactionsRewards,
}
